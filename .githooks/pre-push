#!/usr/bin/env bash
# Tracked pre-push hook. Enabled per-clone via `make install-hooks`
# (which sets core.hooksPath = .githooks).
set -euo pipefail

# Run from the repo root regardless of where git invokes us from.
cd "$(git rev-parse --show-toplevel)"

exec make --no-print-directory check-videos
