<% wrap_layout :default do %>
	<%= partial "partials/banner" %>
	<header>
		<h2><%= current_page.title %></h2>
		<p>
			<%= current_page.date.strftime("%b %Y") %> - <%= partial("partials/tags", :locals => { :current_page => current_page })%>
		</p>
	</header>
	<%= yield %>
<% end %>
